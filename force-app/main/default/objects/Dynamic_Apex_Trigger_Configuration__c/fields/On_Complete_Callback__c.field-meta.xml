<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>On_Complete_Callback__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>Use this to identify the class or method to run when the trigger conditions are met. If there is an error here, the trigger will not be generated, so make sure to follow proper Apex syntax.</description>
    <externalId>false</externalId>
    <inlineHelpText>Use this to identify the class or method to run when the trigger conditions are met. If there is an error here, the trigger will not be generated, so make sure to follow proper Apex syntax.</inlineHelpText>
    <label>On Complete Callback</label>
    <length>100</length>
    <required>true</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
